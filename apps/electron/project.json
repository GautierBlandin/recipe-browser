{
  "name": "electron",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "root": "apps/electron",
  "projectRoot": "apps/electron",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "mkdir -p ./output",
          "cp -R ../desktop/dist/* ./output/"
        ],
        "cwd": "{projectRoot}",
        "parallel": false
      },
      "dependsOn": [
        "desktop:build"
      ]
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx electron .",
        "cwd": "{projectRoot}"
      },
      "dependsOn": [
        "build"
      ]
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node build-scripts/watch.js",
        "cwd": "{projectRoot}"
      },
      "dependsOn": [
        "build"
      ]
    },
    "package": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx electron-builder --dir",
        "cwd": "apps/electron"
      },
      "dependsOn": [
        "build"
      ]
    }
  },
  "tags": []
}
